import { ShemaItemArticleSelected } from './shema-item-article-selected';

describe('ShemaItemArticleSelected', () => {
  it('should create an instance', () => {
    expect(new ShemaItemArticleSelected()).toBeTruthy();
  });
});
